[project]
name = "oceanicblessings"
version = "0.2.0"
description = "Ω∞v — Living Intelligence & Verification Ecosystem"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project.scripts]
omega = "cli:main"
